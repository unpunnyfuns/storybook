Run tests for the Button component and fix any issues.

Use the Vitest CLI by running `npx vitest run` (not Storybook MCP tools), then fix any failing tests and run `npx vitest run` again until they pass.
