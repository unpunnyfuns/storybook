Run tests for the Button component stories using the Storybook MCP testing tool.

Use the `run-story-tests` tool and report which stories passed or failed.

Ignore a11y violations by calling `run-story-tests` with `{ a11y: false }`.
