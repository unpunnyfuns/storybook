// addon-vitest's run-story-tests starts Vitest from this package's root, so
// the storybook test project must be reachable from the default config.
export { default } from './vitest.storybook.config.ts';
